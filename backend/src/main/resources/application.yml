# filepath: d:\a 2.o\herowiki\backend\src\main\resources\application.yml
spring:
  datasource:
    # Change from in-memory to file-based H2
    url: jdbc:h2:file:D:/a 2.o/HeroWiki/backend/data/herowiki # <<< CHANGE THIS LINE
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  jpa:
    hibernate:
      ddl-auto: update # Keep this
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
  # Remove default security user/pass if not needed, rely on SecurityConfig
  security:
    user:
      name: admin
      password: admin
  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8080
